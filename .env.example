# CxMxC Training — environment example.
# This file is committed. Real values live in .env (gitignored) or in localStorage on-device.
#
# Anthropic API key — used by the in-app AI coach.
# In v1, the key is entered in the Profile screen and stored only in localStorage on this device.
# Do NOT put a real key in this repo.
ANTHROPIC_API_KEY=
